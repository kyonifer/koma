package koma.internal

const val notImplemented = "The active backend does not have an explicit implementation of matrix " +
        "for the selected primitive type and therefore has delegated work to the default implementation. " +
        "The default implementation does not currently support this operation."
